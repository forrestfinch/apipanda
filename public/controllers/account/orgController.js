app.controller("OrgController", [
    "$scope",
    "$location",
    "$log",
    function ($scope, $location, $log) {
        'use strict';

        $log.debug("Org Controller Initialized");
    }]);
