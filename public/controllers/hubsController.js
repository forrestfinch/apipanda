app.controller("HubsController", [
    "$scope",
    "$location",
    "$log",
    function ($scope, $location, $log) {
        'use strict';

        $log.debug("Hubs Controller Initialized");
    }]);
