app.controller("ResetController", [
    "$scope",
    "$location",
    "$log",
    function ($scope, $location, $log) {
        'use strict';

        $log.debug("Reset Controller Initialized");
    }]);
