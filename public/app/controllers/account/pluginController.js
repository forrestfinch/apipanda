app.controller("PluginController", [
    "$scope",
    "$location",
    "$log",
    function ($scope, $location, $log) {
        'use strict';

        $log.debug("Plugins Controller Initialized");
    }]);
