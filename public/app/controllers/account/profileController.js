app.controller("ProfileController", [
    "$scope",
    "$location",
    "$log",
    function ($scope, $location, $log) {
        'use strict';

        $log.debug("Profile Controller Initialized");
    }]);
