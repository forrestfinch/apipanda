app.controller("HubController", [
    "$scope",
    "$location",
    "$log",
    function ($scope, $location, $log) {
        'use strict';

        $log.debug("Hub Controller Initialized");
    }]);
