app.controller("DashController", [
    "$scope",
    "$location",
    "$log",
    function ($scope, $location, $log) {
        'use strict';

        $log.debug("Dash Controller Initialized");
    }]);
